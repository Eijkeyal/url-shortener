services:

  redis:
    image: redis:latest
    container_name: redis-url-shortener
    ports:
      - "6379:6379"


  backend:
    build: .
    container_name: url-shortener-backend
    ports:
      - "8080:8080"
    depends_on:
      - redis